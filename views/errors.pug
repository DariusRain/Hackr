extends index.pug

block content
    body(class="wicked-background")
        div
            h1(style="color: red; background-color: black; opacity: 0.8; text-align:center;") #{data.message}
        footer(style="margin-bottom: 35%;")